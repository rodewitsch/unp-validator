{
  "name": "unp-validator",
  "version": "1.0.1",
  "description": "Utility for unp validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "npm run build && node dist/test/unn.test.js"
  },
  "author": "rodewitsch",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.4.5",
    "typescript": "^5.1.6"
  }
}
